import type { ListingCategory } from "./api";

export const categoryLabels: Record<ListingCategory, string> = {
  BOARD_GAME: "Board game",
  MTG_SINGLE: "MTG single",
  MTG_SEALED: "MTG sealed",
  OTHER: "Other",
};
